# Core: temporal knowledge-graph memory over Neo4j
graphiti-core>=0.3.0

# LLM client for generating the assistant's replies
openai>=1.40.0

# Config
python-dotenv>=1.0.0

pandas>=3.0.3

# Direct Neo4j driver for structured data queries (slots, bookings, patients)
neo4j>=5.0.0
